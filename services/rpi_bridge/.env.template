# Copy this to .env and fill in values
# Install: sudo cp mms-bridge.service /etc/systemd/system/
#          sudo systemctl enable mms-bridge && sudo systemctl start mms-bridge

MQTT_BROKER=localhost
MQTT_PORT=1883
MQTT_USER=bridge
MQTT_PASS=CHANGE_ME_BRIDGE_PASSWORD

GOOGLE_APPLICATION_CREDENTIALS=/home/pi/mms/service-account.json
FIREBASE_RTDB_URL=https://lab-access-f569b-default-rtdb.firebaseio.com
